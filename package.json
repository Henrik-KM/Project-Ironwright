{
  "name": "project-ironwright",
  "version": "1.1.0-prealpha.1",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "python -m http.server 8000 --directory web",
    "check": "node --check web/src/loader.mjs && node --check web/tests/source_loader.mjs && node scripts/check_web_sources.mjs",
    "test": "node --test web/tests/sim.test.mjs",
    "test:browser": "python web/tests/browser_smoke.py",
    "validate": "npm run check && npm test && npm run test:browser && python scripts/validate_tiered_release.py"
  }
}
